import { ScrollView, Text, View } from "react-native";

import { ScreenContainer } from "@/components/screen-container";
import { sovereignRecord } from "@/constants/sovereign-data";

export default function AboutScreen() {
  return (
    <ScreenContainer className="px-5 pb-6">
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 32 }}>
        <Text className="pt-5 text-xs font-bold tracking-[3px] text-primary">ABOUT THE VAULT</Text>
        <Text className="mt-2 text-3xl font-bold text-foreground">Private by default</Text>
        <Text className="mt-2 text-base leading-6 text-muted">Sovereign Vault is a local-first viewer for the identity records you supplied.</Text>

        <View className="mt-7 rounded-2xl border border-border bg-surface p-5">
          <Text className="text-lg font-semibold text-foreground">Loaded source files</Text>
          <Text className="mt-1 text-sm leading-5 text-muted">These filenames are recorded in the imported bundle.</Text>
          <View className="mt-4 gap-3">
            {sovereignRecord.sourceFiles.map((file) => (
              <View key={file} className="flex-row items-center rounded-xl bg-background px-3 py-3">
                <View className="mr-3 h-2 w-2 rounded-full bg-primary" />
                <Text className="font-medium text-foreground">{file}</Text>
              </View>
            ))}
          </View>
        </View>

        <View className="mt-4 rounded-2xl bg-primary/10 p-5">
          <Text className="text-lg font-semibold text-foreground">Privacy model</Text>
          <Text className="mt-2 text-sm leading-5 text-muted">The current experience is designed to display local records without requiring an account, cloud sync, or an external identity provider. It does not upload the supplied files.</Text>
        </View>

        <View className="mt-4 rounded-2xl border border-border p-5">
          <Text className="text-xs uppercase tracking-wider text-muted">App version</Text>
          <Text className="mt-1 text-base font-semibold text-foreground">Sovereign Vault 1.0.0</Text>
          <Text className="mt-4 text-xs uppercase tracking-wider text-muted">Artifact scope</Text>
          <Text className="mt-1 text-base font-semibold text-foreground">Local viewer · read-only presentation</Text>
        </View>
      </ScrollView>
    </ScreenContainer>
  );
}
