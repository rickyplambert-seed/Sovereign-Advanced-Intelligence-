import { ScrollView, Text, View } from "react-native";
import { Link } from "expo-router";

import { ScreenContainer } from "@/components/screen-container";
import { identityFacts, sovereignRecord } from "@/constants/sovereign-data";

export default function RecordsScreen() {
  return (
    <ScreenContainer className="px-5 pb-6">
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 32 }}>
        <Text className="pt-5 text-xs font-bold tracking-[3px] text-primary">RECORDS</Text>
        <Text className="mt-2 text-3xl font-bold text-foreground">Identity & heir record</Text>
        <Text className="mt-2 text-base leading-6 text-muted">A readable view of the supplied status record, kept private on this device.</Text>

        <View className="mt-7 rounded-[26px] bg-surface p-5">
          <View className="flex-row items-center justify-between">
            <Text className="text-xs font-bold tracking-[2px] text-primary">RECLAIMED BY HEIR</Text>
            <View className="rounded-full bg-success/15 px-3 py-1"><Text className="text-xs font-bold text-success">ACTIVE</Text></View>
          </View>
          <Text className="mt-5 text-2xl font-semibold text-foreground">{sovereignRecord.heritage}</Text>
          <Text className="mt-2 text-sm leading-5 text-muted">The supplied JSON record names this heritage and records {sovereignRecord.owner} as its owner.</Text>
          <View className="mt-5 border-t border-border pt-4">
            <Text className="text-xs uppercase tracking-wider text-muted">Integrity check</Text>
            <Text className="mt-1 text-lg font-semibold text-success">{sovereignRecord.integrity}</Text>
          </View>
        </View>

        <Text className="mb-3 mt-8 text-xs font-bold tracking-[2px] text-muted">IDENTITY FACTS</Text>
        <View className="overflow-hidden rounded-2xl border border-border bg-surface">
          {identityFacts.map((fact, index) => (
            <View key={fact.label} className={index < identityFacts.length - 1 ? "border-b border-border p-4" : "p-4"}>
              <Text className="text-xs uppercase tracking-wider text-muted">{fact.label}</Text>
              <Text className="mt-1 text-base font-semibold text-foreground">{fact.value}</Text>
            </View>
          ))}
        </View>

        <View className="mt-6 rounded-2xl bg-primary/10 p-4">
          <Text className="text-sm font-semibold text-foreground">What this screen means</Text>
          <Text className="mt-1 text-sm leading-5 text-muted">These values are presented as user-supplied local records. The app does not independently confirm the identity or make a legal determination.</Text>
        </View>

        <Link href="/(tabs)/protocol" className="mt-6">
          <Text className="text-center text-base font-bold text-primary">View dynamic protocol →</Text>
        </Link>
      </ScrollView>
    </ScreenContainer>
  );
}
